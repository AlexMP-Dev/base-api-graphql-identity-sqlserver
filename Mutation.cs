//mutaciones para graphql
public class Mutation
{
    //public async Task<Producto> AddProducto(Producto producto, [Service] CatalogoContext context)
    //{
    //    context.Productos.Add(producto);
    //    await context.SaveChangesAsync();
    //    return producto;
    //}

    // Similar mutations for Cliente and Pedido
}
